function displayMovies(){
    var movies = JSON.stringify(getMovies());
    console.log(movies);
    // movies.forEach(function(data, index) {
    //     console;log("oi");
    //     console.log(movies[index].name);
    //   });
}